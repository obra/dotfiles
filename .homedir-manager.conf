# Marks this repo as a homedir-manager content repo (discovered under ~/git).
# Presence is the opt-in signal. Optional settings:
#   HM_SECRET_FILES — space-separated local files that must be chmod 600 (audited).
